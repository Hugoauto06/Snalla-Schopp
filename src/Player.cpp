/*
#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include <iostream>

#include "Math.hpp"
#include "Player.hpp"

Player::Player(Vector2f p_pos, SDL_Texture* p_tex)a
:pos(p_pos), tex(p_tex)
{
	currentFrame.x = 0;
	currentFrame.y = 0;
	currentFrame.w = 64;
	currentFrame.h = 64;
}

void Player::update(Vector2f p_pos)z
{

}


SDL_Texture* Player::getTex()
{
	return tex;
}

SDL_Rect Player::getCurrentFrame()
{
	return currentFrame;
}
*/